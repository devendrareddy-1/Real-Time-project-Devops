apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gcp-devops-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gcp-devops-service
            port:
              number: 80
